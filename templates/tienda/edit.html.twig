{% extends 'base.html.twig' %}

{% block title %}Edit Tienda{% endblock %}

{% block body %}
    <h1>Edit Tienda</h1>

    {{ include('tienda/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tienda_index') }}">back to list</a>

    {{ include('tienda/_delete_form.html.twig') }}
{% endblock %}
